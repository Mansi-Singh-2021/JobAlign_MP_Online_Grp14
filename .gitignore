## .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Aa][Rr][Mm]/
[Bb]uild[Ll]og.*
artifacts/

## Visual Studio / Rider / VS Code
.vs/
.vscode/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates
*.swp

## Test results
[Tt]est[Rr]esult*/
coverage*.json
coverage*.xml
*.coverage

## NuGet
*.nupkg
*.snupkg
.nuget/
packages/
!packages/build/

## Secrets — never commit these.
## API keys belong in user-secrets locally and environment variables in deployment.
appsettings.*.Local.json
secrets.json
*.pfx
*.key

## SQL Server local files
*.mdf
*.ldf
*.ndf

## OS noise
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.DS_Store

# Claude Code tooling. launch.json is shared on purpose — it is how the app gets started
# and is the same for everyone. Everything else under .claude/ is per-machine session and
# permission state that would only cause churn.
.claude/*
!.claude/launch.json
